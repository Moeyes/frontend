// When the backend adds FSM status, add the enum here.
// export type SubmissionStatus = 'PENDING' | 'APPROVED' | 'REJECTED' | 'FLAGGED' | 'REVISION_REQUESTED';
export type SubmissionStatus = 'PENDING'; // placeholder until backend adds status field
