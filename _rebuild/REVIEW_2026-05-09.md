# Weekly Review — 2026-05-09 (Week 8 Sunday Audit)

**Date:** 2026-05-09  
**Week:** 8 (final rebuild week — all 13 modules complete)  
**Commits reviewed:** 23 (full rebuild week captured; all commits since `git log --since='7 days ago'`)  
**Auditor:** Claude Code (automated grep + spot-check)

---

## Commits this week

| Hash | Message |
|------|---------|
| `e7a3b9d` | smoke: week 8 friday checkpoint |
| `5be62be` | docs: add Prompt 13 — backend gap registry |
| `4019545` | rebuild: team registration mode (Scenario 08) |
| `3a74301` | fix: all frontend-fixable gaps — org override, event filtering, smart redirect, i18n |
| `9a2ab75` | rebuild: cards module |
| `556a3c2` | rebuild: reports module |
| `e966ca6` | rebuild: participation module |
| `e7f487f` | rebuild: registration-flow module |
| `2a3338b` | rebuild: submissions module |
| `b749aea` | rebuild: survey module |
| `6ab7c6c` | rebuild: organizations module |
| `01d8a9d` | rebuild: sports module |
| `27192c6` | rebuild: events module |
| `434af35` | rebuild: users module |
| `a574582` | rebuild: dashboard module |
| `7fcc779` | rebuild: common module |
| `6631090` | rebuild: auth module |
| `493021e` | rebuild: nuke old frontend + foundation layer |
| `3089bd5` | rebuild: establish conventions and module template |
| `a7bd9d2` | rebuild: freeze user scenarios and route map |
| `def2134` | rebuild: freeze backend contract |
| `65f6313` | rebuild: establish conventions and module template |
| `6746a50` | rebuild: freeze user scenarios and route map |

---

## Violations found

| File | Line | Check | Type | Severity | Finding | Verdict |
|------|------|-------|------|----------|---------|---------|
| `modules/events/components/EventStatusBadge.tsx` | 10 | C | `new Date()` usage | — | `const now = new Date()` computes **current time for event status display** (UPCOMING / ACTIVE / ENDED). This is NOT age math — it compares today's date against `event.start_date` / `event.end_date` for a UI badge. **Red Line #3 does not apply here** (RL3 is specifically about computing participant age, not about event date comparisons). | ✅ NOT A VIOLATION |
| `modules/submissions/hooks/useSubmissions.ts` | 6 | G | Unscoped list query | — | `useSubmissions()` called without `organization_id`. However, `/submissions` and `/submissions/[id]` are both `<ProtectedRoute requiredRoles={['admin']}>` — admin sees all submissions by design. No scoping required. | ✅ NOT A VIOLATION |
| `modules/survey/components/BySportSurveyForm.tsx` | 128 | G | Unscoped list query | P2 | `useSurveyEntries()` called without params (fetches all entries). Client-side org+event filter applied at line 133 using `useEffectiveOrgId()`. This is the documented backend gap #7 workaround (`GET /api/participation-per-sport/` lacks server-side filter params). Scoping IS enforced — just client-side. | ⚠️ P2 — KNOWN WORKAROUND (gap #7) |
| `modules/survey/components/SurveyAdminTab.tsx` | 22 | G | Unscoped list query | P2 | Same as above — `useSurveyEntries()` without params, client-side filter by event name (gap #7 workaround). Comment at line 21 documents the intended fix. | ⚠️ P2 — KNOWN WORKAROUND (gap #7) |

**No P0 violations. No P1 violations.**

---

## Check-by-check results

### A — localStorage / sessionStorage (Red Line #4)
```
grep -rn "localStorage\|sessionStorage" --include="*.ts" --include="*.tsx" .
```
**Result: CLEAN** — zero hits. Tokens in HttpOnly cookies only. ✅

### B — Hand-written backend types (Red Line #2)
```
grep -rn "^export interface\|^export type" modules/*/services/
```
**Result: CLEAN** — all `export type` in services are aliases of `components['schemas']['X']` from `_contract/api.types.ts`. No hand-written backend shapes. ✅

Examples verified:
- `events.service.ts`: `export type EventPublic = components['schemas']['EventPublic']` ✅
- `reports.service.ts`: `export type RosterReport = components['schemas']['OrgSportParticipantFullResponse']` ✅
- `cards.service.ts`: `export type CardResponse = components['schemas']['CardResponse']` ✅

### C — new Date() in age math (Red Line #3)
```
grep -rn "differenceInYears\|getAge\|computeAge\|new Date()" modules/
```
**Result: CLEAN** — all age computations use `computeAgeAtEvent(dob, event.start_date)`.

- `DocumentStep.tsx:34` — `computeAgeAtEvent(dateOfBirth, eventStartDate)` ✅
- `OrganizerForm.tsx:61` — `computeAgeAtEvent(dateOfBirth, eventStartDate)` ✅
- `TeamRegistrationPage.tsx:170,340` — `computeAgeAtEvent(row.date_of_birth, eventStartDate)` ✅
- `EventStatusBadge.tsx:10` — `new Date()` for event display status (UPCOMING/ACTIVE/ENDED), NOT age math. Confirmed not a violation. ✅

### D — status in mutation bodies (Red Line #5)
```
grep -rn "status:" modules/*/services/
```
**Result: CLEAN** — zero hits. No `status` field in any PATCH/PUT request body. FSM workaround uses disabled buttons (not PATCH). ✅

### E — Hardcoded strings (Red Line #7)
```
grep -rn ">[A-Z][a-z]" --include="*.tsx" modules/
```
**Result: CLEAN** — zero bare JSX text strings found. All user-facing text uses `t('key')` or `tc('key')`. ✅

### F — i18n key parity
**Result: CLEAN**
- `messages/kh.json`: **561 keys**
- `messages/en.json`: **561 keys**
- Keys only in kh: **0**
- Keys only in en: **0**

Perfect parity. ✅

### G — Federation list queries without scoping (Red Line #6)
Spot-checked 5 hooks:

| Hook | Scoping | Verdict |
|------|---------|---------|
| `useSubmissions` | No params — admin-only route | ✅ Correct (admin sees all) |
| `useRegistrations` | Accepts `organization_id` param; callers pass `useEffectiveOrgId()` | ✅ |
| `useOrganizers` | Passes `organization_id` to `useRegistrations` | ✅ |
| `useSurveyEntries` | No server param (gap #7); client-side filter by `useEffectiveOrgId()` | ⚠️ P2 workaround |
| `useCards` | Requires explicit `orgId` + `eventId`; `enabled: !!orgId && !!eventId` | ✅ |

### H — Console logs / debug code
```
grep -rn "console\.log\|console\.debug\|debugger" modules/
```
**Result: CLEAN** — zero hits. ✅

### I — TODO / FIXME comments
```
grep -rn "TODO\|FIXME\|XXX\|HACK" modules/
```
**Result: CLEAN** — zero hits. Backend gap workarounds are documented with descriptive comments (e.g. `// gap #5 workaround`) rather than TODO markers. ✅

---

## Additional findings

### raw fetch() calls (Red Line #8 adjacency)
Two `fetch()` calls exist outside the typed client:

| File | Line | Reason | Verdict |
|------|------|--------|---------|
| `modules/reports/services/reports.service.ts` | 42 | Excel blob download — typed client cannot handle binary responses; URL is relative (`/api/excel/...`) proxied through Next.js middleware which injects auth | ✅ Legitimate exception |
| `modules/registration-flow/services/registration.service.ts` | 99 | Cloudinary direct upload to external CDN using presigned URL — must bypass backend client | ✅ Legitimate exception |

Both have comments explaining the reason. Neither is a Red Line #8 violation.

---

## Violations summary table

| File | Line | Red Line | Severity | Suggested fix |
|------|------|----------|----------|---------------|
| `modules/survey/components/BySportSurveyForm.tsx` | 128 | #6 (scoping) | P2 | When teammate closes gap #7: add `events_id` + `organization_id` params to `useSurveyEntries({ events_id: eventId, organization_id: orgId })`. See Prompt 13 gap #7. |
| `modules/survey/components/SurveyAdminTab.tsx` | 22 | #6 (scoping) | P2 | Same as above — replace client-side filter with server-side params per Prompt 13 gap #7. |

**P0 violations: 0**  
**P1 violations: 0**  
**P2 violations: 2** (both are documented backend gap workarounds, not new drift)

---

## Top 5 priorities for next session

1. **Human browser smoke test** — code verified clean; eyes-on browser testing still needed (10-item checklist in `SMOKE_2026-05-09.md`). Specifically: Khmer text rendering, Cloudinary upload, report Excel download, language switcher.

2. **Backend gap #1 (UserPublic.organization_id)** — Every federation/org user hits the OrgSelectorBanner workaround. Most impactful fix for UX. Run Prompt 13 gap #1 when teammate delivers.

3. **Backend gap #5 (FSM transitions)** — Approve/reject/flag surveys is the core ministry workflow. Run Prompt 13 gap #5 when teammate delivers `/approve`, `/reject`, `/flag` endpoints.

4. **Backend gap #9 (6 missing report endpoints)** — Ministry requires all 8 Khmer reports. Run Prompt 13 gap #9 for each report endpoint as teammate adds them.

5. **W8 Production deploy (Prompt 12)** — RBAC audit, axe-core accessibility, load test (k6), backup/restore drill. Run after browser smoke test passes and at least gaps #1 + #5 are closed.

---

## Health score

```
P0 violations: 0
P1 violations: 0
P2 violations: 2 (both known backend-gap workarounds, not new drift)

HEALTH: 🟢 GREEN
```

**Prompt 12 (Production Deploy) is unblocked from a code-quality perspective.**  
Remaining blockers are operational: browser verification + backend teammate gaps.
