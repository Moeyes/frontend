# Smoke Test — 2026-05-09 (Week 8 Friday)

**Date:** 2026-05-09  
**Week:** 8 (final week — all 13 modules complete)  
**Tester:** Claude Code (code-path + static analysis; browser verify notes per scenario)  
**Backend:** `http://localhost:8000` — UP (42 paths)  
**Frontend:** `http://localhost:3000` — UP (Next.js 16.2.4 Turbopack, 307 → login)  
**Branch:** `rebuild`  
**Build:** `pnpm tsc --noEmit` CLEAN · `pnpm build` PASS · lint 0 errors (2 pre-existing warnings)

---

## Modules rebuilt (all 13 complete)

| Module | Commit | Routes |
|--------|--------|--------|
| auth | `6631090` | login |
| common | `7fcc779` | sidebar, topbar, layout |
| dashboard | `a574582` | /dashboard |
| users | `434af35` | /users, /users/new, /users/[id] |
| events | `27192c6` | /events, /events/new, /events/[id], /events/[id]/surveys |
| sports | `01d8a9d` | /sports, /sports/new, /sports/[id] |
| organizations | `6ab7c6c` | /organizations, /organizations/new, /organizations/[id] |
| survey | `b749aea` | /surveys, /surveys/[id]/by-sport, /by-number, /by-category |
| submissions | `2a3338b` | /submissions, /submissions/[id] |
| registration-flow | `e7f487f` + `4019545` | /register, /register/new, /register/team |
| participation | `e966ca6` | /participation, /participation/new, /participation/[id] |
| reports | `556a3c2` | /reports |
| cards | `9a2ab75` | /cards |

**Total routes: 29**

---

## Per-scenario results

| # | Scenario | Actor | Result | Notes |
|---|----------|-------|--------|-------|
| S01 | Admin creates event, attaches sports | admin | **PASS** | Status badge computed client-side (UPCOMING/ACTIVE/ENDED) — no publish endpoint (gap #3). Sport attachment uses name-match workaround (gap #2). Both workarounds in place. |
| S02 | Admin initiates survey / sees federation entries | admin | **PASS** | Survey list filters client-side by event name (gap #7 workaround). QueryBoundary + empty state present. |
| S03 | Federation submits by-sport survey | user1 | **PASS** | `useEffectiveOrgId()` scoping correct. POST /api/participation-per-sport/ wired. OrgSelectorBanner shown when org_id null. |
| S04 | Federation submits by-number survey | user1 | **PASS** | Aggregates totals to male/female fields. Known frontend-only limitation (no dedicated endpoint needed). |
| S05 | Federation submits by-category survey | user1 | **PASS (partial)** | Category breakdown collected, aggregated to M/F totals before submit. Per-category data lost (gap #6 — `category_id` not in create schema). Workaround documented in code. |
| S06 | Admin reviews surveys (approve/reject/flag) | admin | **NEEDS-BACKEND-FIX** | Submissions list and detail render correctly. ReviewActions buttons disabled with FSM gap banner. Cannot approve/reject/flag until gap #5 closed (no status field, no FSM endpoints). |
| S07 | Federation registers participant (alone mode) | user1 | **PASS** | Age from `computeAgeAtEvent(dob, event.start_date)` ✓ (Red Line #3). Doc rule enforced (< 18 → birth cert, ≥ 18 → NID/passport). Cloudinary upload flow present. |
| S08 | Federation registers participants (team/CSV) | user1 | **PASS** | CSV template download ✓. Parse + per-row validation ✓. Age check via `computeAgeAtEvent` ✓. Sequential POST with progress bar ✓. Document advisory notice per row ✓ (docs must be added individually post-bulk). |
| S09 | Organization registers organizers | user2 | **PASS** | `role='leader'` passed to POST /api/registration/ ✓. Age/doc rule identical to S07 ✓. `useEffectiveOrgId()` scoping ✓. LeaderRole badge shown ✓. |
| S10 | Admin generates Khmer reports | admin | **PASS (2/8)** | RPT-ROSTER-ALL and RPT-NUMBER-LIST download correctly. 6 missing reports show "Backend needed" label (gap #9). Event + org filter required before download button enables ✓. |

**Summary: 8 PASS / 0 FAIL / 1 NEEDS-BACKEND-FIX / 1 PASS-PARTIAL**

---

## Cross-cutting audit results

| Check | Result | Detail |
|-------|--------|--------|
| Red Line #1 — No Backend-V2 edits | ✅ PASS | Zero files modified under Backend-V2/ |
| Red Line #2 — No hand-written API types | ✅ PASS | All contract types from `_contract/api.types.ts` |
| Red Line #3 — Age from event date | ✅ PASS | `computeAgeAtEvent` in DocumentStep.tsx:34, OrganizerForm.tsx:61, TeamRegistrationPage.tsx:170, 340 — `new Date()` absent from age logic |
| Red Line #4 — No localStorage for tokens | ✅ PASS | Zero localStorage/sessionStorage hits in modules/ |
| Red Line #5 — No status in mutation bodies | ✅ PASS | Zero `"status":` in PATCH/PUT mutation bodies |
| Red Line #6 — Federation/org scoping | ✅ PASS | `useEffectiveOrgId()` used in survey, registration-flow, participation, cards |
| Red Line #7 — No hardcoded strings | ✅ PASS | Zero bare JSX strings found |
| Red Line #8 — Idempotency-Key on mutations | ✅ PASS | core/api/client.ts auto-injects on POST/PUT/PATCH. Two legitimate raw fetch() exceptions: blob download (binary response, can't use typed client) + Cloudinary upload (external CDN) |
| Red Line #9 — Loading/empty/error states | ✅ PASS | All 29 routes have loading.tsx + error.tsx. QueryBoundary + PageEmptyState on all lists |
| Red Line #12 — Build quality | ✅ PASS | tsc clean, 0 lint errors, build succeeds |
| i18n parity (kh = en) | ✅ PASS | 561 keys in kh.json = 561 keys in en.json, zero divergence |
| console.log in modules | ✅ PASS | Zero hits |
| ProtectedRoute coverage | ✅ PASS | All 29 portal pages wrapped |

---

## Known backend gaps (blocking or limiting)

| Gap | Scenario | Status |
|-----|----------|--------|
| #2 SportsEventPublic.sports_id missing | S01 | Workaround: name-match in EventSportManager.tsx:64 |
| #3 EventPublic.status + publish endpoint | S01 | Workaround: computed badge from dates |
| #4 ParticipationPerSportCreate.category_id | S05 | Workaround: aggregate to M/F totals |
| #5 FSM endpoints + status on survey entries | S06 | Workaround: disabled buttons + gap banner |
| #7 Participation list lacks filter params | S02 | Workaround: client-side filter by event name |
| #9 6 of 8 report endpoints | S10 | Workaround: disabled cards with "Backend needed" |
| #1 UserPublic.organization_id | S03–S09 | Workaround: cookie override via useEffectiveOrgId() |

See `_rebuild/01_PROMPTS.md` → PROMPT 13 for exact instructions to wire each gap when teammate delivers.

---

## Top 5 priorities for next session (W8 wrap-up)

1. **Browser smoke test (human)** — Walk S01→S09 manually in browser with seed credentials. Code-path verification is done; eyes-on confirmation needed for: form submission UX, Khmer text rendering, Cloudinary upload, report download.

2. **Close gap #1 (UserPublic.organization_id)** — Federation and organization users hit the OrgSelectorBanner cookie workaround on every module. Once teammate adds the field, run Prompt 13 gap #1.

3. **Close gap #5 (FSM transitions)** — Approve/reject/flag on surveys is the most visible missing feature for ministry demo. Depends on teammate adding `status` field + `/approve`, `/reject`, `/flag` endpoints.

4. **W8 Production deploy checklist** — Run Prompt 12: RBAC audit, accessibility pass (axe-core), load test, backup/restore drill, DNS cutover plan.

5. **Sunday weekly review (Prompt 8)** — Run the automated red-line audit over the past week's commits before the production deploy.

---

## Notes for human browser tester

These steps require a browser and seed credentials:

- [ ] Login as admin → verify dashboard KPIs load
- [ ] Login as federation (user1) → verify OrgSelectorBanner shows if org_id null; confirm scoped data
- [ ] Create an event → attach sport → check EventSportManager name-match workaround works end-to-end
- [ ] Submit a by-sport survey as federation → verify counts appear in admin submissions list
- [ ] Register a participant born 2010 → verify birth certificate is required
- [ ] Register a participant born 2000 → verify NID/passport is required
- [ ] Upload CSV with 3 rows (1 valid, 1 invalid DOB format, 1 under-18) → verify per-row status
- [ ] Download RPT-ROSTER-ALL → verify Khmer column headers render correctly in Excel
- [ ] Generate a card for a registered participant → verify photo fallback + badge colors
- [ ] Switch language → verify all Khmer strings flip to English correctly
