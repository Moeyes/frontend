/**
 * Public Group Layout
 * 
 * Layout for public routes (registration, forgot password, etc)
 */

export default function PublicLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
