/**
 * Survey Page
 * 
 * Organization registration survey form
 */

import { SurveyForm } from '@/features/survey';

/**
 * Survey page component
 */
export default function SurveyPage() {
  return (
    <main className="min-h-screen bg-background">
      <SurveyForm />
    </main>
  );
}
