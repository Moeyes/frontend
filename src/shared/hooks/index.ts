/**
 * Shared Hooks
 *
 * Reusable React hooks used across all features
 * Examples: useLocalStorage, useTheme, useMediaQuery, etc.
 */

// Example: export { useLocalStorage } from './useLocalStorage';
// Example: export { useTheme } from './useTheme';
// Example: export { useMediaQuery } from './useMediaQuery';
